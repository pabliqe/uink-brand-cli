/**
 * Meta Tags and Manifest Generator
 * Creates PWA manifest, React components, and HTML partials
 */

import { mkdir, writeFile } from 'fs/promises'
import path from 'path'

/**
 * Generate PWA manifest.json
 */
export async function generateManifest(brandData, outDir) {
  const manifest = {
    name: brandData.name,
    short_name: brandData.name.length > 12 ? brandData.name.substring(0, 12) : brandData.name,
    description: brandData.description,
    start_url: '/',
    display: 'standalone',
    background_color: brandData.colors.background,
    theme_color: brandData.colors.primary,
    orientation: 'portrait-primary',
    icons: [
      {
        src: '/icon-192x192.png',
        sizes: '192x192',
        type: 'image/png',
        purpose: 'any'
      },
      {
        src: '/icon-512x512.png',
        sizes: '512x512',
        type: 'image/png',
        purpose: 'any'
      },
      {
        src: '/icon-512x512-maskable.png',
        sizes: '512x512',
        type: 'image/png',
        purpose: 'maskable'
      }
    ]
  }

  const manifestPath = path.join(outDir, 'manifest.json')
  await writeFile(manifestPath, JSON.stringify(manifest, null, 2))
}

/**
 * Generate React/Next.js component (JSX)
 */
async function generateReactComponent(brandData, outputPath, assetPath, refs) {
  const code = `/**
 * Auto-generated by @pabliqe/uink-brand-cli
 * DO NOT EDIT MANUALLY - Regenerate with: npx uink-brand
 */

export default function BrandMeta() {
  return (
    <>
      {/* Standard SEO */}
      <title>${brandData.siteTitle}</title>
      <meta name="description" content="${escapeJsx(brandData.description)}" />
      <link rel="canonical" href="${brandData.siteUrl}" />
      
      {/* Mobile & PWA */}
      <meta name="viewport" content="width=device-width, initial-scale=1" />
      <meta name="theme-color" content="${brandData.colors.primary}" />
      <link rel="manifest" href="${assetPath}/manifest.json" />
      
      {/* Favicons */}
      <link rel="icon" href="${assetPath}/${refs.faviconPrimary}" sizes="any" />
      ${refs.hasFaviconSvg ? `<link rel="icon" href="${assetPath}/favicon.svg" type="image/svg+xml" />` : ''}
      <link rel="apple-touch-icon" href="${assetPath}/apple-touch-icon.png" />
      
      {/* Open Graph */}
      <meta property="og:title" content="${escapeJsx(brandData.siteTitle)}" />
      <meta property="og:description" content="${escapeJsx(brandData.description)}" />
      <meta property="og:type" content="website" />
      <meta property="og:url" content="${brandData.siteUrl}" />
      <meta property="og:image" content="${brandData.siteUrl}${assetPath}/${refs.ogImage}" />
      <meta property="og:image:width" content="1200" />
      <meta property="og:image:height" content="630" />
      <meta property="og:site_name" content="${escapeJsx(brandData.name)}" />
      
      {/* Twitter Card */}
      <meta name="twitter:card" content="summary_large_image" />
      <meta name="twitter:title" content="${escapeJsx(brandData.siteTitle)}" />
      <meta name="twitter:description" content="${escapeJsx(brandData.description)}" />
      <meta name="twitter:image" content="${brandData.siteUrl}${assetPath}/${refs.ogImage}" />
    </>
  )
}
`

  await writeFile(outputPath, code)
}

/**
 * Generate TypeScript React component
 */
async function generateTypeScriptComponent(brandData, outputPath, assetPath, refs) {
  const code = `/**
 * Auto-generated by @pabliqe/uink-brand-cli
 * DO NOT EDIT MANUALLY - Regenerate with: npx uink-brand
 */

import React from 'react'

export default function BrandMeta(): React.ReactElement {
  return (
    <>
      {/* Standard SEO */}
      <title>${brandData.siteTitle}</title>
      <meta name="description" content="${escapeJsx(brandData.description)}" />
      <link rel="canonical" href="${brandData.siteUrl}" />
      
      {/* Mobile & PWA */}
      <meta name="viewport" content="width=device-width, initial-scale=1" />
      <meta name="theme-color" content="${brandData.colors.primary}" />
      <link rel="manifest" href="${assetPath}/manifest.json" />
      
      {/* Favicons */}
      <link rel="icon" href="${assetPath}/${refs.faviconPrimary}" sizes="any" />
      ${refs.hasFaviconSvg ? `<link rel="icon" href="${assetPath}/favicon.svg" type="image/svg+xml" />` : ''}
      <link rel="apple-touch-icon" href="${assetPath}/apple-touch-icon.png" />
      
      {/* Open Graph */}
      <meta property="og:title" content="${escapeJsx(brandData.siteTitle)}" />
      <meta property="og:description" content="${escapeJsx(brandData.description)}" />
      <meta property="og:type" content="website" />
      <meta property="og:url" content="${brandData.siteUrl}" />
      <meta property="og:image" content="${brandData.siteUrl}${assetPath}/${refs.ogImage}" />
      <meta property="og:image:width" content="1200" />
      <meta property="og:image:height" content="630" />
      <meta property="og:site_name" content="${escapeJsx(brandData.name)}" />
      
      {/* Twitter Card */}
      <meta name="twitter:card" content="summary_large_image" />
      <meta name="twitter:title" content="${escapeJsx(brandData.siteTitle)}" />
      <meta name="twitter:description" content="${escapeJsx(brandData.description)}" />
      <meta name="twitter:image" content="${brandData.siteUrl}${assetPath}/${refs.ogImage}" />
    </>
  )
}
`

  await writeFile(outputPath, code)
}

/**
 * Generate static HTML partial
 */
async function generateHTMLPartial(brandData, outputPath, assetPath, refs) {
  const html = `<!-- Auto-generated by @pabliqe/uink-brand-cli -->
<!-- DO NOT EDIT MANUALLY - Regenerate with: npx uink-brand -->

<!-- Standard SEO -->
<title>${escapeHtml(brandData.siteTitle)}</title>
<meta name="description" content="${escapeHtml(brandData.description)}">
<link rel="canonical" href="${brandData.siteUrl}">

<!-- Mobile & PWA -->
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="theme-color" content="${brandData.colors.primary}">
<link rel="manifest" href="${assetPath}/manifest.json">

<!-- Favicons -->
<link rel="icon" href="${assetPath}/${refs.faviconPrimary}" sizes="any">
${refs.hasFaviconSvg ? `<link rel="icon" href="${assetPath}/favicon.svg" type="image/svg+xml">` : ''}
<link rel="apple-touch-icon" href="${assetPath}/apple-touch-icon.png">

<!-- Open Graph -->
<meta property="og:title" content="${escapeHtml(brandData.siteTitle)}">
<meta property="og:description" content="${escapeHtml(brandData.description)}">
<meta property="og:type" content="website">
<meta property="og:url" content="${brandData.siteUrl}">
<meta property="og:image" content="${brandData.siteUrl}${assetPath}/${refs.ogImage}">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta property="og:site_name" content="${escapeHtml(brandData.name)}">

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="${escapeHtml(brandData.siteTitle)}">
<meta name="twitter:description" content="${escapeHtml(brandData.description)}">
<meta name="twitter:image" content="${brandData.siteUrl}${assetPath}/${refs.ogImage}">
`

  await writeFile(outputPath, html)
}

/**
 * Generate Next.js metadata object (for app router)
 */
async function generateNextMetadata(brandData, outputPath, assetPath, refs) {
  const code = `/**
 * Auto-generated by @pabliqe/uink-brand-cli
 * DO NOT EDIT MANUALLY - Regenerate with: npx uink-brand
 * 
 * Usage in Next.js App Router:
 * import { metadata } from './.og-brand/next-metadata'
 * export { metadata }
 */

import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: '${escapeJsx(brandData.siteTitle)}',
  description: '${escapeJsx(brandData.description)}',
  applicationName: '${escapeJsx(brandData.name)}',
  generator: 'Next.js',
  keywords: ['${escapeJsx(brandData.name)}'],
  authors: [{ name: '${escapeJsx(brandData.name)}' }],
  creator: '${escapeJsx(brandData.name)}',
  publisher: '${escapeJsx(brandData.name)}',
  metadataBase: new URL('${brandData.siteUrl}'),
  alternates: {
    canonical: '/',
  },
  openGraph: {
    title: '${escapeJsx(brandData.siteTitle)}',
    description: '${escapeJsx(brandData.description)}',
    url: '${brandData.siteUrl}',
    siteName: '${escapeJsx(brandData.name)}',
    images: [
      {
        url: '${assetPath}/${refs.ogImage}',
        width: 1200,
        height: 630,
        alt: '${escapeJsx(brandData.name)}',
      },
    ],
    locale: 'en_US',
    type: 'website',
  },
  twitter: {
    card: 'summary_large_image',
    title: '${escapeJsx(brandData.siteTitle)}',
    description: '${escapeJsx(brandData.description)}',
    images: ['${assetPath}/${refs.ogImage}'],
  },
  icons: {
    icon: [
      { url: '${assetPath}/${refs.faviconPrimary}', sizes: 'any' },
      ${refs.hasFaviconSvg ? `{ url: '${assetPath}/favicon.svg', type: 'image/svg+xml' },` : ''}
    ],
    apple: [
      { url: '${assetPath}/apple-touch-icon.png', sizes: '180x180', type: 'image/png' },
    ],
  },
  manifest: '${assetPath}/manifest.json',
  themeColor: '${brandData.colors.primary}',
  viewport: {
    width: 'device-width',
    initialScale: 1,
  },
}
`

  await writeFile(outputPath, code)
}

/**
 * Main meta files generation function
 */
export async function generateMetaFiles(brandData, generateDir, assetDir, refs = {}) {
  await mkdir(generateDir, { recursive: true })

  // Normalize asset path (remove leading ./ or /)
  const assetPath = assetDir.replace(/^\.?\//, '/').replace(/\/$/, '')

  const resolvedRefs = {
    ogImage: refs.ogImage || 'og-image.jpg',
    faviconPrimary: refs.faviconPrimary || 'favicon.ico',
    hasFaviconSvg: Boolean(refs.hasFaviconSvg),
  }

  await generateReactComponent(brandData, path.join(generateDir, 'BrandMeta.jsx'), assetPath, resolvedRefs)
  console.log(`   ✓ BrandMeta.jsx`)

  await generateTypeScriptComponent(brandData, path.join(generateDir, 'BrandMeta.tsx'), assetPath, resolvedRefs)
  console.log(`   ✓ BrandMeta.tsx`)

  await generateHTMLPartial(brandData, path.join(generateDir, 'meta.html'), assetPath, resolvedRefs)
  console.log(`   ✓ meta.html`)

  await generateNextMetadata(brandData, path.join(generateDir, 'next-metadata.ts'), assetPath, resolvedRefs)
  console.log(`   ✓ next-metadata.ts`)

  // Generate README in the output directory
  const readme = `# Auto-Generated Brand Assets

This directory contains auto-generated meta tag components and snippets.

**DO NOT EDIT THESE FILES MANUALLY** - They will be overwritten on the next build.

To regenerate: \`npx uink-brand\`

## Usage

### React/Next.js (JSX)
\`\`\`jsx
import BrandMeta from './.og-brand/BrandMeta.jsx'

export default function App() {
  return (
    <>
      <head>
        <BrandMeta />
      </head>
      <body>...</body>
    </>
  )
}
\`\`\`

### Next.js App Router
\`\`\`typescript
import { metadata } from './.og-brand/next-metadata'
export { metadata }
\`\`\`

### Static HTML
Copy the contents of \`meta.html\` into your \`<head>\` tag.

## Generated Files
- \`BrandMeta.jsx\` - React component
- \`BrandMeta.tsx\` - TypeScript React component
- \`next-metadata.ts\` - Next.js metadata object
- \`meta.html\` - Static HTML snippet
`

  await writeFile(path.join(generateDir, 'README.md'), readme)
  console.log(`   ✓ README.md`)
}

/**
 * Escape utilities
 */
function escapeHtml(text) {
  if (!text) return ''
  return text
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;')
}

function escapeJsx(text) {
  if (!text) return ''
  return text
    .replace(/\\/g, '\\\\')
    .replace(/"/g, '\\"')
    .replace(/\n/g, '\\n')
}
